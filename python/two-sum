class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        hashNums = {}
        for i, v in enumerate(nums):
            diff = target - v
            if diff in hashNums:
                return [hashNums[diff], i]
            hashNums[v] = i
